# Sandbox virtual package

The code in sandbox/ is used in the sandbox isolate.
It is set up as a package with no dependencies.
You cannot import anything outside of sandbox/ in Dart
libraries in this directory.